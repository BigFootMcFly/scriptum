# ---- Pending
☐ @high User -> Notes after deleting note(s), teh main page should be refresehed, currently the deleted notes are still on the main page.
                Also, the 'refresh-note-list' event should be triggered on any actions, that needs it...

☐ @today FrontPage/Note.php - is this used anywhere? if not, remove this

☐ @high In the user domain, if an admin is logged in, it can edit all post from everyone, this should not be,
        - on the admin note list,add a 'details" action as well
        - Add a custom action to open the post on the admin interface for editing
        - mark the full page as admin coloring, but dont give any special permission
        - check if the admin can see other users private notes on the front page (i think not, but better check, maibe even add a test for it)
        - note: the permalink on the notes works, but it uses note id-s, nnot user/slug addressing


☐ @low tempestphp/highlight css does not understands tailwind @apply ...
        Maybe it's time to replace it with a "newer" highlighter, the richeditor highlighter almost handles it well...

☐ @high The "Add new Note" button in the search bar only works on the 'main-page', if the 'notes' page is active, it does not work (the form is not on the page)
        it should be 'disabled' or should navigate to the '/user/notes/create' page like in the listNote action button
        //NOTE: spa - the header is not loaded in the switch between the tabs, so maybe a script ot $dispatch, or something???
        __OR__ move the "Edit Note" form into it's own component and into the header...

☐ @high If the 'New Note' button is pressed, the 'main page' note list gets refresehed, which is extensive, the modal should be shown on client side only,
        use alpinejs here, and maybe the modal should do an authentication before is shown, to check if the user is not logged out in another window...
        NOTE: in this case, the FrontPage->queryNodeList() is called to get the note list...
        @see: https://w3collective.com/modal-dialog-alpine-js/
        NOTE: maybe this modal should be moved to it's own component...

☐ @low Add events and notifications
        ex.: if admin marks a note as 'hidden' or 'restricted', then create an event, which sends a notification to the user for this
                with a uniue id, where the user can ask about it...

☐ @low Admin -> Notes add tabs (deleted, restricted, etc, other "filters")

☐ @low Currently the front page shows the nodes sorted by the 'updated_at','DESC' order without the search, earlier it was 'created_by','DESC',
        think out, which one is better, or add a swithc/butoon/etc...


☐ @low @maybe TipTapJsonContentExtractor::extractContent() maybe should have a tdo as parameter instead of 4 arrays...
        _AND_ the pictograms should be added to that... ex.:
                class TipTapContentDetails
                {
                        public function __construct
                        (
                                public array $result = [],
                                public array $extras = [],
                                public array $autoTags = []
                                pubilc array $pictograms = [],
                        ) {}

                        public static function new(): self
                        {
                                return new static;
                        }
                }


☐ @high MainPage class is used on /user/main-page, but FrontPage has the 'same' queryNodeList() function, One of them may not be used, sort outthose
        NOTE: the MainPage was the first (for testing), the FrontPage waas created to replace the MainPage, but the replacement needs to be done jet...

☐ @maybe In teh Nomad/Notes InfoView maybe add links to the user profile and note lists (filtered table view)

☐ @low @maybe In the table view (and even on the infolist) add pictograms (small icons) to show, that the note has image,code, link, etc. in it...
        AND maybe add big sites favicons to the list which should be atomatically detected like the autotags...
        @see: https://www.flaticon.com/search?word=image
        @see: https://www.flaticon.com/search?word=code


☐ @low Add quick view actiion to notes tables like in proxima backend

☐ @low Add custom color coding into Note InfoView (both nomad and User) to mark deleted messages

☐ @low Add a "Copy to clipbord" button on code blacks!
        see: https://github.com/livewire/flux/discussions/835

☐ @low put a checkbox on the header of the main page for a user, and if in private (one user) mode, add a "green marking and full border to the page,
        and a "Red" for the admin !!!

☐ @low Maybe in the Note model the 'body' should have a mutator, and that should set the 'body_contet' field
        @see: https://laravel.com/docs/12.x/eloquent-mutators#mutating-multiple-attributes
        set: fn (string $value) => [
            'body' => $value,
            'body_content' => static::extractBodyContents($value),
        ],

☐ @high User/Notes table -> edit -> delete if i delete a note, the note list table still shows the note, the table needs to be refreshed !!!

☐ @high In the User/Notes panel, after creating a note and going to the notes table, the new note is not in the list
            note: spa mode maybe...

☐ @low Add testing for TipTapJsonContentExtractor
        $note = Note::find(8);
        $result = [];
        $extras = [];
        $autoTags = [];
        TipTapJsonContentExtractor::extractContent($note->body, $result, $extras, $autoTags);
        dd($result, $extras, $autoTags);


☐ @low Is it a good way to use globalized "unique" slugs? Would it not be better to make a custom "unique"() validation rule and use it?
        - It would be more complicated sql, propably slower,
        + This "magic" manipulation is not good either...

☐ @high When the main controller/livewire full page component gets added and the /notes/{slug} route gets added,
        update te Note->permalink() function to use the route address instead of the currently temporally hard coded value

☐ @high Copy the Tempest/Higfhlight theme values to the CodeBlock editor theme!

☐ @high Update the IsAdmin middleware, currently it is used on every request in the nomad panel, but it may/should not be used that way..

☐ @low @maybe in the registration form when the 'handle' field changes, check for the availability of the handle
            and show an error inmediatelly if it is not available, instead of just when submitted
            and maybe even add a suggestion for som similar available handle (like: 'john-doe' => 'john-doe-42')

# ---- Completed
✔ @low Add a "back to top" button to the page @done(25-09-03 21:20)

✔ @low Admin -> users format email verified at (badge maybe) @done(25-09-03 13:38)

✔ @low Admin -> New User -> Handle should be autofilled lik in the user/register. @done(25-09-03 09:49)


✔ Make a GuestUser helper or action or whatever, and add HasAvatar to User and add 'avatar_url' foield to User @done(25-09-03 09:41)
        and use that guest user in the user-menu.blade.php:9

✔ @critical Add UserPolicy, and reenable '->strictAuthorization()' in nomad panel @done(25-09-02 13:10)
        error: Strict authorization mode is enabled, but no policy was found for [App\Models\User].

✔ @low Add color coding in the User/Notes and Nomad/Notes table for deleted/normal rows like in proxima backend @done(25-08-31 19:12)

✔ @today In the User/Notes View Note should be the same as the admin version... @done(25-08-31 18:37)
        NOTE: the 'Search index' section and the user name was removed, so it is not the same, created a new one

✔ @low User -> top menu rename 'Notes' to 'My Notes'. @done(25-08-31 12:33)

✔ @today Add a users route, without auth can reed the users public notes @done(25-08-31 12:29)

✔ @high In the User page, in the edit/insert forms limit the visibility options to only private/public !!! @done(25-08-29 17:50)


✔ @high in "resources/views/livewire/auth/register.blade.php" generate the "handle" field value on the "name" blur event. @done(25-08-22 10:04)


✔ @high For a guest the admin panel is not accessable (403), but that icludes the login page aswell, the IsAdmin middleware must be tweeked! @done(25-08-21 10:42)
    NOTE: modified it, if the user is not logged in, it redirects to 'login', otherwise if user is not an admin, sends a 403 error.

✔ @high IN the CodeBlock the dark theme is used even if tghe admin panel is in a white theme, make it dynamic!!! @done(25-08-20 21:29)
