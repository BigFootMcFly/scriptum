# ---- Notes

- The Custom TopBar and and <x-front-page.user-menu> was created to be able to handle "guest" users.
        By default, a panel is behind authorisation, or if tht is removed, it has none, and this was the easier way to extend that behaviour.

# ---- Pending
☐ @critical These two are teh "same" jet still different...
        /mnt/devel/Projects/scriptum/src/resources/views/livewire/edit-note-modal-form.blade.php
        /mnt/devel/Projects/scriptum/src/resources/views/filament/traits/modal-note-editor.blade.php

☐ @low The "initalize_environment_file" in the entrypoint needs some work.
        Currently in each run the .env file cannot exists, it is not saved in a docker volume, so it will always be copied,
        and so the app key will always be recreated (this was a chosen decision in proxima, so all of the sessions get invalidated)
        but in this case, all of the MFA app keys (fields marked as encrypted in the User casts list) cannot be decrypted
        after a key regen!
        For now, i added the .env file as a volume to the composer, so it will newer be overwritten (even merked as read only)

☐ @critical Add confirmation for the New Note Form, if the ESC is pressed and the note is not empty, ask for confirmation to leave the form...

☐ @low Add user avatar pictures to the front page and the /users/xxx page

☐ @low @maybe If a note changes on the admin panel, the user panel should be noticed over the change so it can re-render the mote...

☐ @today Add a global viewingMode helper, what can help determine the current mode, so other components/services can use it.

☐ @low The TopBar got the @persist tag, now it works, but gets the following error:
        "Uncaught Snapshot missing on Livewire component with id: XY2C7TVcKpYpHt7LdSEb"
        Try to find a solution for this (currenly works, but it should not be ignored)
        NOTE: this is only a problem with SPA mode.
        NOTE: currently spa mode is disabled, until a solution is found.

☐ @low admin / notes - if search for user/title/etc., the tab badges should reflect the result numbers

☐ @low @maybe add a self statistics to the profile or somewhere, to show info about the user like:
        - registration date, profile picture, all usefull stuff
        - note statistics, count of private/published/etc.
        - maybe if tags are added (and autotags and pictograms) a statistics/accessors of them as well

☐ @low Nomad - Add brand icon in the top left corner after an icon is made...

☐ @low Nomad -> add Global Search for user/notes/etc... (currently the global search is disabled)
        also, for refernce @see: https://proxima.goliath.hu/proxima/backend

☐ @low @maybe @later in the admin /notes table, maybe add caching for the getTabs() queries
        the count() querys are reloaded on each refresh, meybe ther should be cached (like in the proxima)
        NOTE: if i remember correctly, i added caching for this in the proxima backend @see: https://proxima.goliath.hu/proxima/backend
                but maybe the published version has that stripped it as well, need to check...
        NOTE: there maybe already some caching present, need to read trough the source code again...

☐ @low @livewire-error see:https://github.com/livewire/livewire/discussions/7746

☐ @low on the front page, amybe add some possibility for the admin to edit a "bougus" note, without the need to go to "manage" -> table and find it again
        _OR_ add a possible edit link/button on the "/notes/{slug}" page for it, so it is not on the front page, but still more accessible for the admin
        _AND_ maybe mark the page in some awy to pressure, that it is in 'admin' mode....
        _AND_ mark the full page as admin coloring, but dont give any special permission
                which pages? it depends, the "/notes/{slug}" is a good candidate for it
        _AND_ maybe it would be  agood idea, to add an 'admin mode" button on the front page for the admin, with full page color/etc marking...

☐ @low check if the admin can see other users private notes on the front page (i think not, but better check, maibe even add a test for it)
        No, it cannot see, but a test would be a good thing to have for later "changes"

☐ @low tempestphp/highlight css does not understands tailwind @apply ...
        Maybe it's time to replace it with a "newer" highlighter, the richeditor highlighter almost handles it well...

☐ @low Add events and notifications
        ex.: if admin marks a note as 'hidden' or 'restricted', then create an event, which sends a notification to the user for this
                with a uniue id, where the user can ask about it...

☐ @low Currently the front page shows the nodes sorted by the 'updated_at','DESC' order without the search, earlier it was 'created_by','DESC',
        think out, which one is better, or add a swithc/butoon/etc...

☐ @low @maybe TipTapJsonContentExtractor::extractContent() maybe should have a tdo as parameter instead of 4 arrays...
        _AND_ the pictograms should be added to that... ex.:
                class TipTapContentDetails
                {
                        public function __construct
                        (
                                public array $result = [],
                                public array $extras = [],
                                public array $autoTags = []
                                pubilc array $pictograms = [],
                        ) {}

                        public static function new(): self
                        {
                                return new static;
                        }
                }

☐ @maybe In teh Nomad/Notes InfoView maybe add links to the user profile and note lists (filtered table view)

☐ @low @maybe In the table view (and even on the infolist) add pictograms (small icons) to show, that the note has image,code, link, etc. in it...
        AND maybe add big sites favicons to the list which should be atomatically detected like the autotags...
        @see: https://www.flaticon.com/search?word=image
        @see: https://www.flaticon.com/search?word=code

☐ @low Add custom color coding into Note InfoView (both nomad and User) to mark deleted messages

☐ @low Add a "Copy to clipbord" button on code blacks!
        see: https://github.com/livewire/flux/discussions/835

☐ @low put a checkbox on the header of the main page for a user, and if in private (one user) mode, add a "green marking and full border to the page,
        and a "Red" for the admin !!!

☐ @low Maybe in the Note model the 'body' should have a mutator, and that should set the 'body_contet' field
        @see: https://laravel.com/docs/12.x/eloquent-mutators#mutating-multiple-attributes
        set: fn (string $value) => [
            'body' => $value,
            'body_content' => static::extractBodyContents($value),
        ],

☐ @low Add testing for TipTapJsonContentExtractor
        $note = Note::find(8);
        $result = [];
        $extras = [];
        $autoTags = [];
        TipTapJsonContentExtractor::extractContent($note->body, $result, $extras, $autoTags);
        dd($result, $extras, $autoTags);

☐ @low Is it a good way to use globalized "unique" slugs? Would it not be better to make a custom "unique"() validation rule and use it?
        - It would be more complicated sql, propably slower,
        + This "magic" manipulation is not good either...

☐ @high Copy the Tempest/Higfhlight theme values to the CodeBlock editor theme!

☐ @high Update the IsAdmin middleware, currently it is used on every request in the nomad panel, but it may/should not be used that way..

☐ @low @maybe in the registration form when the 'handle' field changes, check for the availability of the handle
            and show an error inmediatelly if it is not available, instead of just when submitted
            and maybe even add a suggestion for som similar available handle (like: 'john-doe' => 'john-doe-42')

# ---- Completed
✔ @low The view "http://localhost:8000/users/benedek-gerzson" is to narrow on desktop, maybe add a min-width for mx+ sizes @done(25-10-16 18:54)

✔ @critical On ViewNotePage and ViewUserNotesPage pages the modal edit is not present !!! @done(25-10-16 18:49)
        and the footer is on the wrong place

✔ @critical On recreating the container, all uploaded data is lost, even custom profile images!!! @done(25-10-16 18:33)
        and all images in the notes !!!
        The storage volume is now passed in with the docker compose file, it is necessary, not optional...

✔ @critical On the PROD page the version is "Version: b672" (no tag) @done(25-10-16 18:32)
        Added full checkout to the workflow.

✔ @critical The permalinki in the header of the note is in bad position if the note is editable by the user !!! @done(25-10-16 18:31)
        Removed it completly.

✔ @high Add two factor authentication! @done(25-10-14 18:17)

✘ Try run in the entrypoint: @cancelled(25-10-14 18:15)
        composer install \
        --no-interaction \
        --no-dev \
        --optimize-autoloader
        NOTE: after updating composer packages, the error could not be recreated.

✔ @high if logged in on two pages, and logging out in one, on teh other clicking on the "Manage my notes" pbutton gives an error: @done(25-10-12 12:18)
        "Call to a member function notes() on null"
                file: app/Filament/User/Resources/Notes/Pages/ListNotes.php :36
        This should result a 403!
        NOTE: added check and abort(403...)

✔ @high Add a notification bar to the bottom of the top bar to logged in but unverified users, with a button to resend the email @done(25-10-12 11:35)

✔ @high Change notes policy, so only verified users can create new notes! @done(25-10-12 11:35)
        Also modify the big add new note button according to this!

✔ @low rename 'front-page' slug -OR- remove it and the whole 'user' path, it looks terrible... @done(25-09-16 10:45)
        NOTE: if the user panel provider path is set to '', some things break, fix them and be the 'front-page' be the '' page

✔ @low @maybe add a samll summary under the search bar ex.: total notes: X, own notes: Y (base on search result) @done(25-09-15 13:42)

✔ @low on the Users / Notes page there is no real reson of the privte/public viewing mode changing, only the private part is relevant... @done(25-09-15 13:41)
        if we move to the notes page, update the stats to the private stats !!!
        NOTE: stats/badges shows "private" viewing mode values, the Viewing Mode button is kept in place

✔ @high @today The frontpage search is broken, if in private viewing mode a search for "nem" returns 5 result on the front page, @done(25-09-15 13:40)
        but 6 in the Notes table (the one missing from the front page is a "public" note,
        so the scope is bad (there is an orWhere, which propably missing the precluding where))
        - IMPORTANT - the stats are on Public version, even in Private mode
        - IMPORTANT - the User/notes the tab badges are not correct

✔ @high if the viewmode is changed, the 'page' parameter should be reset and the page shoud be scrolled to the top @done(25-09-13 13:58)

✔ @low NOTE: I tried to set the viewmode to admin, and it did not work... AND then it hit me, i was logged in with a non-admin test acc :D @done(25-09-12 14:49)
        This may be a bit confusing for the testing, but works...
        SOLUTION: this is the intended behaviour, "magic" viweing mode changes not a good solution, but i dont want to add another button or selector for it

✔ @low on the admin notes table, color code the 'hidden' and 'restricted' fields as well... @done(25-09-06 23:11)
        NOTE: i already added a class for 'hidden' somewhere, with a colors and blur...

✔ @low User / notes ->limit the lengths of the title and slug columns, it can make the table scroll out of view... @done(25-09-06 22:53)
        NOTE: did limit to 50 characters, on small screen or mobil it still scrolls, so this is of limited use...

✔ @high User / notes -> update the list if the global search is updated @done(25-09-06 22:39)

✔ @low User / notes -> add a notice above the table if the "with deleted record" filter is not set, @done(25-09-06 22:07)
                that records are hidden from teh listing, and change the filter to seee _ALL_ records

✔ @high User / Notes - if the note is hidden/restricted, but the user is admin, the view/edit actions are present and the note is editable, @done(25-09-06 20:39)
        that should not be the case!!! Those records should only be editable in the admin interface !!!
        SOLUTION: added visible() to the actions, in the uzser view not even admin can use those actions.

✔ @low Admin -> Notes add tabs (deleted, restricted, etc, other "filters") @done(25-09-06 18:41)

✔ @high MainPage class is used on /user/main-page, but FrontPage has the 'same' queryNodeList() function, One of them may not be used, sort outthose @done(25-09-06 17:59)
        NOTE: the MainPage was the first (for testing), the FrontPage waas created to replace the MainPage, but the replacement needs to be done jet...
        SOLUTION: removed MainPage

✔ @high In the user domain, if an admin is logged in, it can edit all post from everyone, this should not be, - DONE @done(25-09-06 17:58)
        - on the admin note list,add a 'details" action as well - DONE
        - Add a custom action to open the post on the admin interface for editing - MOVED TO SEPARATE TODO
        - note: the permalink on the notes works, but it uses note id-s, nnot user/slug addressing - DONE

✔ @low @maybe User -> "Manage My Notes" maybe remove the "New note" button fromn the table (headerActions), and use the global insert button, @done(25-09-06 17:18)
        just don't forget to update the list after a new record is added.
        @see: NewNoteButton.php:22
        SOLUTION: the global button is used for now, no need to have different buttons for the same thing

✔ @high The "Add new Note" button in the search bar only works on the 'main-page', if the 'notes' page is active, it does not work (the form is not on the page) @done(25-09-06 16:11)
        it should be 'disabled' or should navigate to the '/user/notes/create' page like in the listNote action button
        //NOTE: spa - the header is not loaded in the switch between the tabs, so maybe a script ot $dispatch, or something???
        __OR__ move the "Edit Note" form into it's own component and into the header...
        SOLUTION: added an enabled state and disabling it on the "/users/notes" page

✔ @high Add a permalink to the User/nomad -> notes -> detail infoview @done(25-09-06 16:09)

✔ @low Add quick view actiion to notes tables like in proxima backend @done(25-09-06 11:27)

✘ @high User/Notes table -> edit -> delete if i delete a note, the note list table still shows the note, the table needs to be refreshed !!! @cancelled(25-09-06 11:14)
        NOTE: same, canot reproduce this eighter

✘ @high In the User/Notes panel, after creating a note and going to the notes table, the new note is not in the list @cancelled(25-09-06 11:12)
            note: spa mode maybe...
            NOTE: it works, as it should, cannot reproduce faulti behavior, maybe some old bad solutions caused this, that were removed since...

✔ @high When the main controller/livewire full page component gets added and the /notes/{slug} route gets added, @done(25-09-06 11:06)
        update te Note->permalink() function to use the route address instead of the currently temporally hard coded value

✔ @high If the 'New Note' button is pressed, the 'main page' note list gets refresehed, which is extensive, the modal should be shown on client side only, @done(25-09-06 11:02)
        use alpinejs here, and maybe the modal should do an authentication before is shown, to check if the user is not logged out in another window...
        NOTE: in this case, the FrontPage->queryNodeList() is called to get the note list...
        @see: https://w3collective.com/modal-dialog-alpine-js/
        NOTE: maybe this modal should be moved to it's own component...
        SOLUTION: moved the 'Nwe Note' button into the top-bar

✘ @high User -> Notes after deleting note(s), the main page should be refresehed, currently the deleted notes are still on the main page. @cancelled(25-09-06 11:00)
        Also, the 'refresh-note-list' event should be triggered on any actions, that needs it...
        It works now, after navigating back to the main page the page is reloaded, so this should not happen, must have been some bug elswhere...

✔ @high User -> main page ->edit note -> after save the note list is not refreshed, the note has the old values... @done(25-09-06 10:36)


✔ @low If a note is updated, the note should be refreshed, not the note list (it is possible, that a noot is not even on the page... (not likely, but still...)) @done(25-09-06 10:33)

✔ @today FrontPage/Note.php - is this used anywhere? if not, remove this @done(25-09-03 22:30)
        Deleted it.

✔ @today Some Notes have a slug starting wint "#/", which should not be a thing... @done(25-09-03 22:16)
        check for how that happens and when and change it!!!
        NOTE: removed the settet on the slug, now works, but the bas notes must be updated.. DONE

✔ @low Add a "back to top" button to the page @done(25-09-03 21:20)

✔ @low Admin -> users format email verified at (badge maybe) @done(25-09-03 13:38)

✔ @low Admin -> New User -> Handle should be autofilled lik in the user/register. @done(25-09-03 09:49)


✔ Make a GuestUser helper or action or whatever, and add HasAvatar to User and add 'avatar_url' foield to User @done(25-09-03 09:41)
        and use that guest user in the user-menu.blade.php:9

✔ @critical Add UserPolicy, and reenable '->strictAuthorization()' in nomad panel @done(25-09-02 13:10)
        error: Strict authorization mode is enabled, but no policy was found for [App\Models\User].

✔ @low Add color coding in the User/Notes and Nomad/Notes table for deleted/normal rows like in proxima backend @done(25-08-31 19:12)

✔ @today In the User/Notes View Note should be the same as the admin version... @done(25-08-31 18:37)
        NOTE: the 'Search index' section and the user name was removed, so it is not the same, created a new one

✔ @low User -> top menu rename 'Notes' to 'My Notes'. @done(25-08-31 12:33)

✔ @today Add a users route, without auth can reed the users public notes @done(25-08-31 12:29)

✔ @high In the User page, in the edit/insert forms limit the visibility options to only private/public !!! @done(25-08-29 17:50)


✔ @high in "resources/views/livewire/auth/register.blade.php" generate the "handle" field value on the "name" blur event. @done(25-08-22 10:04)


✔ @high For a guest the admin panel is not accessable (403), but that icludes the login page aswell, the IsAdmin middleware must be tweeked! @done(25-08-21 10:42)
    NOTE: modified it, if the user is not logged in, it redirects to 'login', otherwise if user is not an admin, sends a 403 error.

✔ @high IN the CodeBlock the dark theme is used even if tghe admin panel is in a white theme, make it dynamic!!! @done(25-08-20 21:29)
